apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namePrefix: element-call-

resources:
- deployment.yaml
- service.yaml
- ingress.yaml


configMapGenerator:
  - name: element-call-config
    files:
      - config.json
