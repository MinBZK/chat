apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namePrefix: element-web-

resources:
- deployment.yaml
- service.yaml
- ingress.yaml
- ingress2.yaml


configMapGenerator:
  - name: element-web-config
    files:
      - config.json
